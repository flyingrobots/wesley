# Generator JS Test Workspace

Stub workspace used when running `pnpm --filter @wesley/generator-js test`. Keeps package-local fixtures isolated under `src/`.

Typical usage:

```bash
pnpm --filter @wesley/generator-js test
```
