# Stack Witness 0001 Fixture Extension

This descriptor package models a small external consumer of Wesley-generated
artifacts. It is intentionally local and hermetic.

The fixture stands in for the shape Echo and jedit exercise together: an
authored schema, operation metadata, declared footprints, and codec-vector
expectations. Wesley may use it for compiler and emitter coverage, but the
runtime behavior belongs to external hosts and modules.
