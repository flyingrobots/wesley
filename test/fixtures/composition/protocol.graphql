extend schema @wes_package(name: "test.protocol")
extend schema @wes_import(from: "core.graphql")
extend schema @wes_import(from: "core-ext.graphql")

type WidgetEvent {
  widget: Widget
  action: String!
}
