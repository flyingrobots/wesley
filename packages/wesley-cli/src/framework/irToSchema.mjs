/**
 * Re-export from @wesley/core.
 * The canonical implementation now lives in core; this file exists
 * for backward compatibility with existing CLI command imports.
 */
export { irToSchema } from '@wesley/core';
