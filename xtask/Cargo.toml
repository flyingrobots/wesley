[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
description = "Wesley repository automation"
publish = false

[dependencies]
ninelives = "0.2"
semver = "1.0"
serde_json = "1.0"
tokio = { version = "1", features = ["rt", "time"] }
toml = "0.8"
