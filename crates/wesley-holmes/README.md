# wesley-holmes

A Rust library in the [Wesley](https://github.com/flyingrobots/wesley#readme) workspace that validates the
law-evidence artifacts Wesley publishes. It is not published to crates.io and
adds no commands to `wesley`.

It depends on none of the other Wesley crates. It is versioned with them so that
the workspace moves together.
