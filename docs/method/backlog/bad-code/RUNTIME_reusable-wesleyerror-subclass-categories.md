# RUNTIME reusable WesleyError subclass categories

Legacy `TASKS.md` still named the missing reusable error taxonomy above
`WesleyError`.

Done when:
- reusable categories such as validation, conflict, and resource errors are explicitly defined or deliberately rejected
- migrated call sites stop inventing parallel one-off hierarchies
- the result lines up with the repo's evolving Wesley/Holmes error surface
