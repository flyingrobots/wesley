# TRANSMUTE v2 plugin capability contract and native Supabase cutover

Legacy `ROADMAP.md` still described the move from shimmed legacy emitters
toward a native plugin capability contract and native Supabase transmutation.

Done when:
- plugin capability descriptors are explicit and testable
- the native Supabase path replaces legacy shim wiring
- plugin conformance can be asserted without custom one-off logic per package
