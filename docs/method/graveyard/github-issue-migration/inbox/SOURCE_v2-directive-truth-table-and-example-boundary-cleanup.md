# SOURCE v2 directive truth table and example boundary cleanup

Legacy `ROADMAP.md` still carried a Phase 0 promise to stop lying about
directive semantics and mixed example validity.

Done when:

- the repo has one explicit directive truth table covering shipped, deferred, and dead semantics
- example fixtures separate happy-path examples from deliberate negative cases
- docs and runtime stop presenting proposed directive behavior as if it already shipped
